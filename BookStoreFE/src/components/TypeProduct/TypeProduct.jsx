import React from "react";

  const TypeProduct = ({name}) => {
    return (
        <div style={{padding: '0 10px', cursor:"pointer",fontSize:"12px"}}>{name}</div>
    )
  }
  export default TypeProduct